{
  "type": "mysql",
  "host": "${DB_URL}",
  "port": "${DB_PORT}",
  "username": "${DB_USER}",
  "password": "${DB_PASS}",
  "database": "${DB_NAME}",
  "entities": [ "src/models/*.ts" ],
  "synchronize": false
}
